# cr P0 notes

This file records the current product judgement for the lightweight review workflow.

## Current P0

1. Changed-file hierarchy
   - Status: implemented.
   - Why: before reading a file, the reviewer needs to know where it sits in the repository.
   - Surface: `cr diff` and `cr review` both start with a changed-file tree.
2. Per-file change hunks with context
   - Status: implemented.
   - Why: line counts and symbols are not enough to answer "what actually changed in this file?"
   - Surface: `cr review` prints compact `changes:` hunks for each changed file.
3. Compact file-purpose hint
   - Status: implemented.
   - Why: human reviewers need a quick answer to "what is this file roughly for?" before reading details.
   - Surface: `cr outline` and code-file sections in `cr review` print `purpose:`.
4. Review filters
   - Status: implemented.
   - Why: noisy working trees make terminal output hard to scan; reviewers often narrow to code or one directory first.
   - Surface: `cr diff` and `cr review` support `--code` and optional path filters.
5. Deleted/renamed file display polish
   - Status: implemented.
   - Why: file lifecycle changes are easy to misread if they look like ordinary modified files.
   - Surface: deleted files are marked in `diff`/`review`; renamed summaries preserve the old path when Git reports a rename.
6. Staged changes support
   - Status: implemented.
   - Why: reviewers often stage a subset of AI changes before review; `cr` should not miss index changes.
   - Surface: `cr diff --staged` and `cr review --staged` inspect staged/index changes and compose with `--code` and path filters.
7. Large-change summary ergonomics
   - Status: implemented.
   - Why: humans scan before reading; large reviews need totals and one-line file focus before full detail output.
   - Surface: `cr review` starts with a compact `Summary:` table.
8. Output depth controls
   - Status: implemented.
   - Why: large reviews often need a quick scan before reading full hunks.
   - Surface: `cr review --summary` shows summary/tree only; `cr review --no-hunks` keeps purpose/modified/outline without diff hunks.
9. Machine-readable review output
   - Status: implemented.
   - Why: review facts should be reusable by scripts, AI prompts, and other terminal tools without scraping human text.
   - Surface: `cr review --json` emits structured summary and file data, and composes with `--summary`, `--no-hunks`, `--code`, and `--staged`.
10. Mixed staged/unstaged awareness
   - Status: implemented.
   - Why: reviewers can easily miss index changes when reading unstaged changes, or vice versa.
   - Surface: `cr diff` and `cr review` print a note when the other side has matching changes; JSON includes `other_changes`.
11. Configurable hunk context
   - Status: implemented.
   - Why: compact hunks are good defaults, but some changes need either zero context for AI prompt budget or wider context for human reasoning.
   - Surface: `cr review --context N` controls per-file hunk context for both terminal and JSON output, defaulting to 2 lines.
12. Combined local-change view
   - Status: implemented.
   - Why: staged and unstaged changes often belong to the same review session; forcing separate reads slows down daily use.
   - Surface: `cr diff --all` and `cr review --all` use `git diff HEAD` to show staged and unstaged tracked changes together, composing with `--code`, path filters, `--json`, and hunk controls.
13. First changed-line anchors
   - Status: implemented.
   - Why: after scanning a terminal review, humans often want to jump directly to the changed place without searching inside the file.
   - Surface: `cr review` summary rows include `path:line` anchors, file-tree leaves include `line N`, detail headers include `@ path:line`, and JSON includes `anchor` plus `first_changed_line`.
14. Untracked file awareness
   - Status: implemented.
   - Why: AI coding often creates new files before they are staged; hiding them makes the review incomplete.
   - Surface: default `cr diff`/`cr review` and `--all` include untracked files from `git ls-files --others --exclude-standard`; text files are shown as full-file additions and JSON reports `status: "untracked"`.
15. Binary and large-file guardrails
   - Status: implemented.
   - Why: generated assets, archives, and oversized text files can flood terminal review output and make real code changes harder to see.
   - Surface: binary, non-UTF-8, and over-200 KB untracked files stay visible in summaries but inline hunk content is replaced with a short omitted-content note.
16. Path-based risk hints
   - Status: implemented.
   - Why: lockfiles, config files, and generated outputs are easy to skim past but often need deliberate human confirmation.
   - Surface: `cr review` summary/tree/details and JSON expose conservative `lockfile`, `config`, and `generated` hints based on changed paths.
17. Line-numbered hunk rendering
   - Status: implemented.
   - Why: reviewers often need to quote or discuss a specific changed line after reading terminal output.
   - Surface: `cr review` hunk rows include old/new line-number columns while preserving the original `+`, `-`, and context text.
18. Installable command entry point
   - Status: implemented.
   - Why: a daily CLI tool should be runnable as `cr`, not only as `PYTHONPATH=src python3 -m cr`.
   - Surface: `setup.py` exposes the `cr` console script at `cr.cli:main`, avoids isolated build dependency downloads for older pip editable installs, and README install instructions verify `cr --help`.
19. Baseline comparison mode
   - Status: implemented.
   - Why: reviewers often need to inspect an already-committed branch against `main` or another known ref, not only unstaged local edits.
   - Surface: `cr diff --base REF` and `cr review --base REF` reuse the same summary, anchors, hunk rendering, code filters, and JSON output against a named branch or commit.
20. Explicit two-ref comparison mode
   - Status: implemented.
   - Why: reviewers sometimes need to inspect `main..feature` or a commit range without checking out the right-hand ref and disturbing their current worktree.
   - Surface: `cr diff --range OLD..NEW` and `cr review --range OLD..NEW` reuse the normal summary/tree/hunk/JSON output while reading code outlines from `NEW`.
21. Large-review navigation sorting
   - Status: implemented.
   - Why: when many files change, humans often want to inspect risk files or largest churn first instead of reading Git order linearly.
   - Surface: `cr review --sort risk`, `--sort churn`, and `--sort path` reorder summary/detail/JSON output while the default `--sort git` preserves existing behavior.
22. Review session handoff
   - Status: implemented.
   - Why: reviewers often need to paste a compact change package into AI or team chat without manually trimming terminal tables and hunks.
   - Surface: `cr review --prompt` emits Markdown with review instructions, totals, per-file risk/purpose/focus metadata, and fenced compact diff hunks; it composes with sort, context, code, path, staged, base, and range controls.
23. Lightweight per-file extraction
   - Status: implemented.
   - Why: after scanning a large summary, reviewers need a fast way to expand exactly one file without copying a long path from the terminal.
   - Surface: `cr review` summary rows include `idx`, and `cr review --pick N` selects the Nth file after the current filters and sort; it composes with full terminal, `--summary`, `--json`, and `--prompt` output.
24. Lightweight review-state handoff
   - Status: implemented.
   - Why: large reviews often happen over several terminal passes; reviewers need a way to carry "already read" files forward without a database or IDE session.
   - Surface: `cr review --seen PATH` marks files as seen in terminal, JSON, and prompt output, and `--remaining` filters seen files after the current filters and sort.
25. Final daily-use audit
   - Status: implemented.
   - Why: before stopping, the tool should be checked against the original daily workflow instead of simply accumulating features.
   - Surface: verified local-change review, changed-file hierarchy, per-file hunks, file purpose, anchors, risk triage, large-review navigation, prompt handoff, review-state handoff, baseline/range review, and editable install with current tests and smoke commands.
26. In-session browse filtering
   - Status: implemented.
   - Why: once `cr browse` became the default TUI, large reviews still needed a fast way to jump from a long changed-file list to the relevant path without linear navigation.
   - Surface: `cr browse` supports `/` filter input, non-TTY `/query` and `filter query`, `c` / `clear`, active filter status, filtered numeric selection, filtered next/previous navigation, editor opening, and refresh selection clamping.
27. Browser module deepening
   - Status: implemented.
   - Why: continuing to add TUI behavior inside `src/cr/cli.py` made the CLI module shallow; the parser had to know about terminal raw keys, screen rendering, filter state, and editor handoff.
   - Surface: `src/cr/ui/browser.py` now owns the interactive browse module interface and implementation, while `src/cr/cli.py` delegates `cmd_browse` to `run_browser(args)`.
28. Package taxonomy
   - Status: implemented.
   - Why: once the project grew past a few files, placing Git, source parsing, review rendering, and terminal UI modules directly under `src/cr` made the package interface too abstract.
   - Surface: `src/cr` now exposes architecture through `vcs`, `source`, `review`, and `ui` packages, with `CONTEXT.md` documenting where new behavior should live.
29. Review workflow deepening
   - Status: implemented.
   - Why: `src/cr/cli.py` was still carrying review selection, seen/pick handling, JSON/prompt/terminal output branching, and file detail rendering after the package split.
   - Surface: `src/cr/review/workflow.py` owns `cr review` execution, while `src/cr/review/changes.py` owns reusable changed-file facts shared with `cr diff`.
30. Simple default command ergonomics
   - Status: implemented.
   - Why: daily use should be `cr` first, with optional browse filters only when needed; `cr --context 0` should not force users to remember the `browse` subcommand.
   - Surface: CLI prepends `browse` when the first argument is an option, so `cr --code`, `cr --sort risk`, and `cr --context 0` enter the interactive browser directly.
31. Shared review facts
   - Status: implemented.
   - Why: after `cr review` moved into `cr.review`, `cr browse` still carried duplicate change selection, sorting, hunk rendering, code-file detection, and modified-symbol rules.
   - Surface: `src/cr/ui/browser.py` now reuses `src/cr/review/changes.py` for review-scope facts and keeps only interaction state, terminal rendering, and editor opening behavior.
32. Stable browser screen layout
   - Status: implemented.
   - Why: once build output became an in-session background task, bottom-log updates and raw-key navigation started competing for the same terminal rows; this made the product feel unstable right when it should behave like a small IDE replacement.
   - Surface: `cr browse` now treats the screen as four layers: help/context, main content, background task panel, and input prompt. Build output stays in a 5-10 line task panel above the prompt, panel refresh does not clear the main screen, and raw-key navigation no longer emits extra newlines.
33. Build task controls
   - Status: implemented.
   - Why: a workbench cannot only start long-running builds; users need a way to stop a bad run, rerun after a fix, and read the outcome without leaving review context.
   - Surface: `cr browse` supports `: build`, `: stop` / `: cancel`, and `: rerun` / `: rebuild`. The build panel distinguishes running, stopping, stopped, succeeded, failed, failed-to-start, and idle states.
34. Review workspace scope clarity
   - Status: implemented.
   - Why: once the browser can inspect worktree, staged changes, commit history, and ref ranges, users need to know exactly which review workspace is active and switch between them without restarting.
   - Surface: `cr browse` displays `Scope: ...` for worktree, staged, all local changes, base refs, explicit ranges, recent commits, and selected commits. The command prompt supports `worktree`, `staged`, `all`, `base REF`, and `range OLD..NEW` and resets view-local selection/filter/scroll state on scope switches.
35. Browser command list
   - Status: implemented.
   - Why: once `cr browse` has many in-session commands, users need an in-tool way to discover them without reading README or triggering unknown-command noise.
   - Surface: `cr browse` supports `commands`, `cmds`, and `help commands`; in raw mode, `:` followed by empty input or `?` opens the same command list. The list groups navigation, review scope, build task, file, and session commands, and `b` returns to the file list.
36. Build process group lifecycle
   - Status: implemented.
   - Why: stopping only the build entry process can leave child build processes running, which risks continued log output after the user returns to normal review navigation.
   - Surface: interactive background builds run in an isolated process group. `: stop` / `: cancel` terminates that process group first, falls back to parent-process termination if group cleanup fails, and keeps the build panel's stopped/succeeded/failed lifecycle semantics stable.
37. Build stop hard-kill escalation
   - Status: implemented.
   - Why: graceful process-group termination is still only a request; real build tools may ignore or delay it, leaving the workbench stuck in stopping while logs continue.
   - Surface: `: stop` / `: cancel` records the stop request time. If the build is still running after a short grace period, `cr browse` sends one force-kill signal to the build process group, falls back to the parent process when no group id exists, and keeps duplicate escalation logs out of the bottom panel.
38. Browser workspace persistence
   - Status: implemented.
   - Why: a workbench should preserve a user's review context across terminal sessions instead of dropping them back at the default file list every time.
   - Surface: default `cr browse` saves scope, filter, selected path/index, and list/file layer under `.git/cr/browse-state.json` on clean exit. Later default sessions restore that workspace; explicit scope flags and pathspecs win over saved state.
39. Browser review progress markers
   - Status: implemented.
   - Why: when a review spans many files or a previous commit, users need a cheap way to know what has already been scanned without leaving the browser or maintaining a separate checklist.
   - Surface: `cr browse` supports `m` / `seen` / `done` to mark the selected file seen, `todo` / `unseen` / `unmark` to clear it, `remaining` to show only unreviewed files, and `allfiles` / `show all` to return to the full list. The file tree shows `[x]` / `[ ]`, file headers show `seen` / `todo`, and progress state persists in `.git/cr/browse-state.json`.
40. Browser frame rendering
   - Status: implemented.
   - Why: build logs, temporary prompts, navigation, and future task panels must not compete for terminal output ownership; otherwise the workbench feels unstable exactly when it is trying to replace IDE workflows.
   - Surface: raw-key `cr browse` now has a `BrowserFrame` render state. Full redraw records the active layout and task-panel snapshot; build panel partial refreshes only write while that frame is still valid; `:` and `/` temporary input force the next update to restore the full frame.
41. Executable command palette
   - Status: implemented.
   - Why: a workbench should not make users memorize every command once actions span review, build, scope switching, file operations, and session control.
   - Surface: raw-key `commands` mode is now a command palette. It shows executable actions grouped by purpose, supports ↑/↓ / j/k selection, Enter executes the selected command through the normal command path, and it avoids accidentally opening the selected review file from the command page.
42. Command palette search
   - Status: implemented.
   - Why: once the command palette becomes the action entry point, users need to type a few letters and narrow the action list instead of scanning every group.
   - Surface: raw-key `commands` mode supports `/` to filter executable commands by group, label, command, or description; `c` / `clear` clears the command filter without touching the file path filter; Enter executes the selected filtered command through the normal command path.
43. Task panel history
   - Status: implemented.
   - Why: build output should feel like an IDE task/output panel, not a transient log tail; after reruns users still need to see whether the previous build succeeded, failed, or stopped.
   - Surface: the browser keeps session-local `TaskRecord` history for completed tasks. The bottom task panel shows compact recent task results alongside current task status and latest logs, records each completed task once, retains history across reruns, and keeps the history out of `.git/cr/browse-state.json`.
44. Browser layer ownership
   - Status: implemented.
   - Why: the product is moving toward a terminal IDE replacement, so page regions need ownership rules. Build logs, command feedback, and navigation must not all write to stdout independently.
   - Surface: raw-key `cr browse` treats the page as context/status, main content, background task panel, and input prompt layers. Open-file feedback, invalid choices, and unknown commands render in the context/status layer; they mark the frame dirty so build output waits for a full redraw instead of patching a stale bottom panel.
45. Product navigation breadcrumbs
   - Status: implemented.
   - Why: the product hierarchy is `Review Scope -> Changed Files -> File Detail`, but users need to see that hierarchy in the interface, not only in docs.
   - Surface: the context/status layer now renders breadcrumbs such as `Scope: worktree > Files`, `Scope: commit abcdef12 > Files`, and `Scope: worktree > Files > src/Sample.ts`. Recent commits stay as `Scope: recent commits` because that screen is still a scope picker, not a file list.
46. Scope home
   - Status: implemented.
   - Why: Review Scope is the product's first layer; users should not have to memorize scattered commands to find worktree, staged, all local changes, commits, base, and range entry points.
   - Surface: `scopes` / `scope` opens a Review Scope Home. Users can select worktree, staged, all local changes, or recent commits with keyboard navigation and Enter. Base/range are shown as parameterized command hints, preserving existing `: base REF` and `: range OLD..NEW` flows.
47. Task command breadth
   - Status: implemented.
   - Why: replacing IDE-like daily workflow needs more than build; users need to run tests and lint without leaving review context.
   - Surface: `cr browse` supports `: build`, `: test` / `: tests`, and `: lint` in the same bottom task panel. `: stop` / `: cancel` stop the current task, and `: rerun` / `: rebuild` rerun the most recent task kind. Build keeps the DouyinHarmony default; test/lint use explicit `--test-cmd` / `CR_TEST_CMD` and `--lint-cmd` / `CR_LINT_CMD`.
48. Task state naming
   - Status: implemented.
   - Why: after Task Panel started handling build/test/lint, build-specific runtime names made the architecture harder to extend and easier to misunderstand.
   - Surface: the current background task runtime is now `TaskState`, `BrowserState.task`, `_poll_task`, `_record_completed_task`, and `_task_panel_*`. Build-specific naming remains only where behavior is truly build-specific, such as `_build_command`.
49. Browser page model
   - Status: implemented.
   - Why: the product hierarchy is Review Scope, Changed Files, File Detail, and cross-layer overlays, but the browser implementation still spoke mostly in raw `mode` strings such as `list`, `file`, and `commands`.
   - Surface: `BrowserPage` now names Scope Home, Commit Picker, Changed Files, File Detail, and Command Palette. `BrowserState.page` is the primary state field, while `mode` remains a compatibility property and persisted workspace values stay unchanged.
50. Navigation module deepening
   - Status: implemented.
   - Why: after page naming became explicit, the browser main loop still owned many raw page transition assignments and reset rules, making back/open/next behavior harder to change safely.
   - Surface: `BrowserNavigation` owns page transition intent and local reset rules for Changed Files, File Detail, Scope Home, Commit Picker, and Command Palette. It does not load Git data, switch review scopes, or render output; page history is covered by the later Real page stack P0.
51. ReviewWorkspace deepening
   - Status: implemented.
   - Why: replacing IDE-like workflows needs a stable review workspace model; active scope, changed files, selected commit, previous scope, filter/progress, and persisted selected path were still coordinated directly by `browser.py`.
   - Surface: `ReviewWorkspace` now owns active Review Scope state, changed-file loading, filter/progress state, selected file state, selected commit, previous scope, and workspace-state data mapping. Browser file I/O and rendering remain outside the workspace module.
52. Command dispatch deepening
   - Status: implemented.
   - Why: after navigation and workspace behavior moved behind named modules, the browser main loop still recognized command strings directly, making command aliases and parameter parsing easy to duplicate or accidentally change.
   - Surface: `BrowserCommandAction` and `parse_browser_command` now translate raw-key aliases, line-mode commands, parameterized commands, numeric selections, and unknown input into stable browser actions before `run_browser` executes them.
53. Command action execution deepening
   - Status: implemented.
   - Why: once command parsing had a seam, the browser main loop still owned every parsed action branch, so each new workbench operation would continue to increase one high-risk loop.
   - Surface: `BrowserCommandExecutor` now executes parsed browser actions and returns `BrowserActionResult` loop control. `run_browser` keeps input prompts, sentinels, workspace save-on-exit, and redraw scheduling, but no longer owns the full action branch table.
54. Task runtime extraction
   - Status: implemented.
   - Why: build/test/lint already behave like IDE task panel operations, but command resolution, process lifecycle, output capture, stop escalation, rerun, and history were still owned by `browser.py`.
   - Surface: `cr.ui.tasks` now owns Task Panel runtime behavior and `TaskState` / `TaskRecord`; `browser.py` uses that runtime while keeping terminal layout and panel rendering.
55. Task configuration presets
   - Status: implemented.
   - Why: a workbench should know a project's normal build/test/lint commands without requiring repeated CLI flags or shell environment setup.
   - Surface: `cr.ui.tasks` now reads `.cr/tasks.json` for project-local build/test/lint defaults. CLI args and environment variables override presets; DouyinHarmony build default remains the final build fallback.
56. File action breadth
   - Status: implemented.
   - Why: review work repeatedly needs small file operations such as copying a path, copying a stable anchor, or revealing the file without leaving the browser.
   - Surface: `copy path`, `copy anchor`, and `reveal` now run through `BrowserCommandAction`, the command palette, and `BrowserCommandExecutor`; `cr.ui.file_actions` owns clipboard and OS file-browser subprocess details.
57. Task preset diagnostics
   - Status: implemented.
   - Why: project task presets are intentionally tolerant, but users need a quick way to see which command source won and whether `.cr/tasks.json` is malformed.
   - Surface: `tasks` now reports build/test/lint sources through `BrowserCommandAction`, the command palette, and `BrowserCommandExecutor`; `cr.ui.tasks` owns the source explanation and preset diagnostic text.
58. Real page stack
   - Status: implemented.
   - Why: terminal workbench navigation should behave like an IDE: after jumping into details, commands, or scope surfaces, users need predictable back/forward history instead of only hierarchy fallback.
   - Surface: `BrowserNavigation` now owns in-session page snapshots, back/forward stacks, forward clearing on new branches, and history reset on Review Scope reloads.
59. File action configuration
   - Status: implemented.
   - Why: copy/reveal fallbacks differ across terminals and desktops; users need to adapt selected-file actions to their local toolchain without changing code.
   - Surface: `--copy-cmd` / `CR_COPY_CMD` and `--reveal-cmd` / `CR_REVEAL_CMD` now configure copy/reveal actions. `cr.ui.file_actions` owns template expansion, env lookup, platform fallback, and subprocess execution.
60. Task preset schema help
   - Status: implemented.
   - Why: project task presets are useful only if users can discover the `.cr/tasks.json` shape from inside the workbench instead of hunting through docs.
   - Surface: `tasks help` now shows `.cr/tasks.json`, supported `build` / `test` / `lint` string commands, command precedence, and a JSON example. Malformed preset diagnostics also point to `tasks help`.
61. Review notes
   - Status: implemented.
   - Why: replacing IDE-like review workflows needs a lightweight way to leave context on a file without switching tools or editing a separate checklist.
   - Surface: `note TEXT` sets one per-file review note for the selected changed file, `note` clears it, Changed Files rows show a compact `note` marker, File Detail shows the full note, and default browse workspace persistence saves/restores notes.
62. Editor handoff diagnostics
   - Status: implemented.
   - Why: file actions are only useful if users can understand why open/copy/reveal failed in their terminal or desktop environment.
   - Surface: `file actions` reports open/copy/reveal command sources as CLI, env, platform, or missing. Open/copy/reveal failure messages include the attempted source and command summary.
63. Command palette organization
   - Status: implemented.
   - Why: as the workbench command set grows, users need search feedback and useful result ordering instead of a flat contains-filter list.
   - Surface: command palette filters now show match counts and rank command/label matches before group and description-only matches while preserving catalog order for ties and unfiltered mode.
64. Review notes summary
   - Status: implemented.
   - Why: per-file notes are useful only if the user can quickly scan all pending review thoughts without opening every file.
   - Surface: `notes` summarizes workspace review notes without changing page, selection, scope, or task state. Current changed-file notes follow review order; persisted notes outside the active changes are appended by path.
65. Editor action extraction
   - Status: implemented.
   - Why: selected-file actions should not split editor handoff knowledge between `browser.py` and `cr.ui.file_actions`; future editor configuration and diagnostics need one deep module.
   - Surface: `cr.ui.file_actions` now owns open/copy/reveal command templates, CLI/env/platform source resolution, subprocess launch, and source-aware failures. `browser.py` passes selected-file path, first changed line, and configured command strings.
66. Review notes copy
   - Status: implemented.
   - Why: review notes become much more useful when the user can carry the current review context into chat, an AI prompt, or a PR comment.
   - Surface: `copy notes` / `notes copy` copies the same ordered summary shown by `notes`, reusing `--copy-cmd` / `CR_COPY_CMD`. Empty note sets report `No review notes to copy.` without launching clipboard commands.
67. Review notes search
   - Status: implemented.
   - Why: once notes become a real review workspace, users need to narrow them by path or text without scanning the whole summary.
   - Surface: `notes QUERY` filters review notes with case-insensitive substring matching over repo-relative paths and note text, preserving current changed-file ordering plus sorted extra persisted paths. It does not change page, selection, scope, task state, file filters, or `copy notes`.
68. Filtered review notes copy
   - Status: implemented.
   - Why: after narrowing notes with `notes QUERY`, users should be able to carry only that filtered context into chat, an AI prompt, or a PR comment.
   - Surface: `copy notes QUERY` copies the same filtered summary shown by `notes QUERY`, reusing `--copy-cmd` / `CR_COPY_CMD`. No-match filtered copies report `No matching review notes to copy.` without launching clipboard commands, while `notes copy` continues to copy the full summary.
69. Browser prompt handoff
   - Status: implemented.
   - Why: terminal review becomes much more useful when the user can send the current scope or current file to AI/chat without leaving the browser and reconstructing `cr review --prompt` manually.
   - Surface: `copy prompt` copies prompt-ready Markdown for the current visible changed files, and `copy prompt file` copies the selected visible file only. Both reuse `cr.review` prompt rendering, include matching review notes for copied files, use existing copy command configuration, and avoid launching clipboard commands for empty scopes.
70. Command catalog extraction
   - Status: implemented.
   - Why: as the workbench grows toward IDE-like command surfaces, command help groups, executable palette entries, filtering/ranking, and command surface row rendering should not live inside the already-large browser orchestration module.
   - Surface: `cr.ui.command_catalog` owns command catalog data, executable command palette entries, command filtering/ranking, command list lines, and command palette screen lines. `browser.py` keeps command filter/selection/scroll state and frame placement, preserving user-visible command behavior.
71. Workspace persistence extraction
   - Status: implemented.
   - Why: workspace persistence is stable product behavior but its file path, schema wrapping, JSON tolerance, and restore/save eligibility should not remain mixed into the browser loop and rendering module.
   - Surface: `cr.ui.workspace_persistence` owns `.git/cr/browse-state.json` path construction, schema version wrapping/validation, tolerant JSON read/write, and default-session restore/save eligibility. `ReviewWorkspace` still interprets product state, while `browser.py` only orchestrates startup/exit calls and live state synchronization.
72. Browser Frame extraction
   - Status: implemented.
   - Why: screen layout and Task Panel presentation are core to the workbench feeling stable, but they should not stay mixed with product page rendering and command-loop orchestration as IDE-like operations grow.
   - Surface: `cr.ui.frame` owns Browser Frame layout, terminal line fitting, Task Panel line presentation, and Task Panel-only partial refresh output. `browser.py` keeps prompt-input interpretation, command execution, frame composition, and session orchestration through compatibility wrappers.
73. Browser prompt save
   - Status: implemented.
   - Why: clipboard handoff is not enough for large reviews, remote terminals, missing clipboard commands, or workflows where the review package should be opened by another AI/chat/tool from disk.
   - Surface: `save prompt [PATH]` writes prompt-ready Markdown for the current visible changed files, defaulting to `.cr/handoff/review-prompt.md`. `save prompt file [PATH]` writes only the selected visible file, defaulting to `.cr/handoff/review-prompt-file.md`. Both reuse the same `cr.review.prompt` Markdown and review-note filtering as `copy prompt`.
74. Browser Page Content extraction
   - Status: implemented.
   - Why: Scope Home, Changed Files, Commit Picker, empty states, and File Detail rendering had become stable product-page behavior, but it was still mixed into browser session orchestration, prompt input, frame composition, and action side effects.
   - Surface: `cr.ui.page_content` owns page prompt labels, help lines, breadcrumbs/context, changed-file tree/list rows, commit rows, empty state text, file detail lines, cache keys, and scroll-window calculations. `browser.py` keeps compatibility wrappers while owning prompt-input interpretation, command execution, frame composition, selected-file side effects, and workspace startup/exit.
75. Browser Input extraction
   - Status: implemented.
   - Why: raw-key reads, temporary line input, idle task ticks, EOF/interrupt sentinels, and escape-sequence mapping are stable terminal protocol behavior, but they were still mixed into browser session orchestration.
   - Surface: `cr.ui.input` owns raw-key availability checks, browse command reads, filter/command query reads, raw escape-sequence mapping, idle tick, EOF, and interrupt sentinels. `browser.py` keeps compatibility wrappers while interpreting returned tokens and owning frame dirty/redraw recovery, workspace save-on-exit, command execution, selected-file side effects, and workspace startup/exit.
76. Selected File Actions extraction
   - Status: implemented.
   - Why: open/copy/reveal/note/prompt-file workflows all depend on the current selected changed file, first changed line, notes, prompt handoff selection, platform helpers, and cache invalidation, but were still mixed into browser command execution.
   - Surface: `cr.ui.selected_file_actions` owns selected-file open, copy path, copy anchor, copy/save diff snippet, reveal, set/clear note, prompt handoff selection, and copy/save prompt handoff messages. `BrowserCommandExecutor` keeps parsed action routing and UI feedback placement, while `cr.ui.file_actions`, `cr.ui.handoff`, `cr.review.snippet`, and `cr.review.prompt` keep their existing responsibilities.
77. Selected-file index actions
   - Status: implemented.
   - Why: replacing IDE-like workflows needs the user to review a file, then stage or unstage that exact file without leaving the terminal workbench.
   - Surface: `stage` and `unstage` operate on the current Changed Files selection through command parsing, command palette, `cr.ui.selected_file_actions`, and `cr.vcs.git`. Successful actions refresh the active mutable local Review Scope; base/range/commit scopes remain read-only.
78. Change source badges
   - Status: implemented.
   - Why: after staging/unstaging from inside the workbench, users need to know whether each local file is staged, unstaged, or mixed without switching scopes.
   - Surface: `cr.vcs.git.FileChange.source` now carries `staged`, `unstaged`, or `mixed` for local changes. Browser Changed Files rows render that badge while base/range/commit comparison scopes stay badge-free.
79. Change source filter
   - Status: implemented.
   - Why: source badges are more useful when users can temporarily focus the Changed Files view on staged, unstaged, or mixed files, especially inside `all local changes`.
   - Surface: `source staged`, `source unstaged`, `source mixed`, and `source all` filter the current Changed Files view through `ReviewWorkspace.source_filter`. The filter composes with path filters and remaining-only mode, and scope switches clear it.
80. Change source summary
   - Status: implemented.
   - Why: source badges and filters still require scanning the file tree to understand the current staged/unstaged/mixed mix; Changed Files should expose that overview near the file count.
   - Surface: `cr.ui.page_content` now renders a `Sources:` line for visible local source facts, omitting zero-count sources and hiding the summary when comparison scopes have no local source facts.
81. Scope Home counts
   - Status: implemented.
   - Why: Review Scope is the first product layer, so users should be able to see whether Worktree, Staged, All local changes, and Recent commits contain anything before entering them.
   - Surface: Scope Home rows now show temporary live counts for Worktree, Staged, All local changes, and Recent commits. `browser.py` samples these counts on Scope Home entry/refresh, `cr.ui.page_content` renders them, and workspace persistence does not store them.
82. Commit Picker change summary
   - Status: implemented.
   - Why: Recent commits are first-layer Review Scope candidates; users need to know each commit's size before entering it and drilling into Changed Files.
   - Surface: `cr.vcs.git.recent_commits()` now populates per-commit file count and added/deleted totals from Git numstat data. Commit Picker rows render summaries such as `2 files, +10 -3` without changing commit selection or selected-commit diff behavior.
83. Commit Picker filter
   - Status: implemented.
   - Why: Recent commits are a first-layer selector, so `/` should narrow commits there instead of leaking the old Changed Files filter behavior and jumping back to files.
   - Surface: Commit Picker now supports `/`, `/QUERY`, and `filter QUERY` over loaded recent commits, matching hash, date, subject, and displayed change summary. `c` clears only the commit filter while preserving Changed Files filters, and numeric/Enter selection operates on the filtered list.
84. Commit Picker module extraction
   - Status: implemented.
   - Why: Commit Picker has become a first-layer Review Scope selector with stable rules for matching, filtering, and filtered selection. Keeping those rules inside Page Content made rendering own product model behavior and blurred the three-layer workbench hierarchy.
   - Surface: `cr.ui.commit_picker` owns commit search text, loaded-commit filtering, and filtered selection helpers. `cr.ui.page_content` keeps row rendering, counts, empty states, and scroll-window text; `browser.py` keeps prompt routing, filter state, and scope-selection side effects.
85. Selected-file diff copy
   - Status: implemented.
   - Why: replacing IDE workflows needs a lightweight way to share the current file's diff context without copying a full AI prompt handoff or manually selecting terminal text.
   - Surface: `copy diff` copies a compact Markdown review snippet for the selected visible file, including hunk context, anchor, seen state, review note, purpose/focus, and risk metadata. `cr.review.snippet` owns snippet text, `cr.ui.selected_file_actions` owns selected-file choice and copy execution, and `BrowserCommandExecutor` owns status feedback.
86. Review Notes module extraction
   - Status: implemented.
   - Why: Review Notes are now a stable workspace concept, but summary ordering, path/text filtering, empty states, and copy status messages were still private browser helpers. That kept product rules inside the orchestration module and made future note-based workflows harder to extend.
   - Surface: `cr.ui.review_notes` owns all-note summary/search/copy behavior. `browser.py` delegates existing `_review_note_lines` and `_copy_review_notes` wrappers to it, preserving user-visible `notes`, `notes QUERY`, `copy notes`, and `copy notes QUERY` behavior.
87. Selected-file diff save
   - Status: implemented.
   - Why: terminal clipboard handoff can be unreliable, and replacing IDE workflows needs a durable selected-file diff artifact without copying a full AI prompt handoff.
   - Surface: `save diff [PATH]` saves the same compact Markdown review snippet as `copy diff`, defaulting to `.cr/handoff/review-diff.md`. `cr.review.snippet` owns snippet text, `cr.ui.selected_file_actions` owns selected-file choice and save workflow, `cr.ui.handoff` owns path resolution and UTF-8 writes, and `BrowserCommandExecutor` owns status feedback.
88. File Detail hunk navigation
   - Status: implemented.
   - Why: replacing IDE review workflows needs fast movement between changed blocks inside a large file, not only line-by-line or page scrolling.
   - Surface: `next hunk` / `]` and `prev hunk` / `[` jump between rendered diff hunk headers inside the current File Detail view. `cr.ui.file_detail_navigation` owns hunk detection and target scroll calculation; `BrowserCommandExecutor` applies the scroll and status feedback while preserving scope, selection, filters, notes, progress, and task state.
89. File Detail hunk editor handoff
   - Status: implemented.
   - Why: after navigating to a specific hunk, users should be able to open that exact changed block in their editor instead of jumping back to the file's first changed line.
   - Surface: `open hunk` works only in File Detail, resolves the active rendered hunk's new-file start line, and reuses the existing `open` command configuration and failure messages.
90. File Detail hunk copy
   - Status: implemented.
   - Why: reviewers often need to paste just the currently inspected change block without copying the whole selected-file snippet.
   - Surface: `copy hunk` works only in File Detail, copies the active rendered hunk as line-numbered Markdown review context, and preserves scope, selection, filters, notes, progress, and task state.
91. File Detail hunk action deepening
   - Status: implemented.
   - Why: as File Detail gains IDE-like operations, hunk workflows should not keep growing `BrowserCommandExecutor`; future UI/runtime rewrites need action behavior behind a stable selected-file interface.
   - Surface: `open hunk` and `copy hunk` now delegate selected hunk workflow to `cr.ui.selected_file_actions`, while `browser.py` keeps page checks, cached File Detail line retrieval, status feedback, and redraw control.
92. File Detail refresh preservation
   - Status: implemented.
   - Why: an IDE-like terminal workbench should let users reload current changes without losing the file detail they are reading.
   - Surface: ordinary `refresh` / `r` keeps File Detail when the selected path still exists in refreshed visible changes, clamps file scroll to the refreshed content height, resets page history, and falls back to Changed Files with a status message when the file disappears. Stage/unstage refresh behavior still returns to Changed Files.
93. File Detail text find
   - Status: implemented.
   - Why: while reading a large File Detail, users need to jump to a symbol, property, or changed text without leaving the current file context.
   - Surface: `find TEXT` works only in File Detail, searches rendered File Detail text case-insensitively, strips ANSI color codes before matching, jumps to the first matching body line, and keeps Review Scope, selection, notes, progress, and task state.
94. File Detail repeated text find
   - Status: implemented.
   - Why: after jumping to the first match in a large File Detail, users need to continue through repeated symbol or text occurrences without retyping the query or leaving the file.
   - Surface: `next match` and `prev match` work only in File Detail after a non-empty `find TEXT`, search rendered text case-insensitively from the current file scroll, wrap within the selected file, and keep Review Scope, selection, notes, progress, and task state.
95. File Detail current line actions
   - Status: implemented.
   - Why: after scrolling or searching to a specific File Detail row, users need an IDE-like way to open or share that exact new-file line instead of falling back to the file's first changed line or the whole hunk.
   - Surface: `open line` and `copy line` work only in File Detail, resolve the current rendered row's new-file line number from hunk headers, context rows, or added rows, report an empty state for deleted/metadata rows, and preserve Review Scope, selection, notes, progress, task state, page, and scroll.
96. File Detail changed-line navigation
   - Status: implemented.
   - Why: large hunks can contain many context rows, and users need to move between actual added/deleted rows without stepping through every line or only jumping at hunk granularity.
   - Surface: `next change` and `prev change` work only in File Detail, jump between rendered added/deleted rows with wraparound, ignore context/hunk/metadata rows, and preserve Review Scope, selection, notes, progress, task state, and page.
97. File Detail current change copy
   - Status: implemented.
   - Why: after jumping to a specific added/deleted row, reviewers often need to paste just that one change into AI chat, team chat, or a review note without copying the whole hunk.
   - Surface: `copy change` works only in File Detail, copies the current actual changed row as compact Markdown review context, includes a `path:line` anchor for added rows, uses the old line number for deleted rows, reports an empty state for non-change rows, and preserves Review Scope, selection, notes, progress, task state, page, and scroll.
98. File Detail current change notes
   - Status: implemented.
   - Why: when using the workbench as an IDE replacement, reviewers need to mark a suspicious line immediately without manually copying its line number into a file-level note.
   - Surface: `note change TEXT` works only in File Detail on actual added/deleted rows, appends `line N: TEXT` or `old line N: TEXT` to the selected file's existing review note, preserves existing file note text, reports empty states for non-change rows or missing File Detail, and keeps Review Scope, selection, progress, task state, page, and scroll.
99. Done-next review flow
   - Status: implemented.
   - Why: continuous review should not require repeating `m` then `n`/Enter for every file; the workbench needs an IDE-like "done, go next" action.
   - Surface: `done next` and `seen next` mark the current visible changed file as seen and advance to the next visible file, staying in Changed Files when started there and opening the next File Detail when started in File Detail. In `remaining` mode, it selects the file that moves into the current visible index after the current file is filtered out, avoiding skipped unreviewed files.
100. Review progress action deepening
   - Status: implemented.
   - Why: review progress is Review Workspace behavior; keeping mark/unmark/advance selection rules inside browser action execution would make future progress operations harder to reason about.
   - Surface: `ReviewWorkspace` now owns selected-file progress operations for mark, unmark, and mark-and-advance. `BrowserCommandExecutor` delegates those rules to the workspace and keeps only page-aware feedback and File Detail reopening behavior.
101. Contextual action bar
   - Status: implemented.
   - Why: as the workbench grows toward IDE replacement, users should see the most useful current-page actions without memorizing the full command list.
   - Surface: raw-key `cr browse` now renders a one-line contextual action bar under the scope/status line. Changed Files, File Detail, Scope Home, Commit Picker, and Command Palette each show page-specific high-frequency actions. `cr.ui.page_content` owns the action text, while Browser Frame keeps the line fitted into the stable screen layout.
102. Task output handoff
   - Status: implemented.
   - Why: Task Panel output should behave like an IDE output panel; users need to carry build/test/lint logs into AI/chat/files without manual terminal selection.
   - Surface: `copy task` copies the current task's type, status, command, and captured output. `save task [PATH]` writes the same Markdown text to disk, defaulting to `.cr/handoff/task-output.md`. Empty task state reports a no-output message without copying or writing. Task Runtime owns handoff text; Browser Action Execution owns clipboard/save side effects.
103. Task output detail page
   - Status: implemented.
   - Why: the bottom Task Panel is intentionally only 5-10 lines, but users still need to inspect long build/test/lint logs without leaving the TUI or losing their code-review context.
   - Surface: `task output` / `output` opens a current-task output page showing task status, command, and captured output lines with independent `task_scroll`. `↑/↓`, `PgUp/PgDn`, and `Home/End` scroll the page; `b` returns through page history. Ordinary pages keep Task Panel-only refresh while tasks run, while Task Output Page redraws main content so live logs stay visible. No historical task-log browsing, diagnostics parsing, lifecycle changes, or output persistence were added.
104. Task output find
   - Status: implemented.
   - Why: after long logs became readable inside Task Output Page, users still needed IDE-like text search to jump to failures or keywords without manual paging.
   - Surface: Task Output Page now supports `find TEXT`, `next match`, and `prev match` over current captured task output. Search is case-insensitive, strips ANSI styles, uses independent `task_find_text`, and does not search task history or parse diagnostics. `cr.ui.text_search` owns generic rendered-text matching so Task Output does not depend on File Detail navigation internals.
105. Task Problems page
   - Status: implemented.
   - Why: after logs became searchable, the next IDE-like loop is jumping from task output file anchors directly into code without manually copying `path:line` text.
   - Surface: `problems` / `task problems` opens a current-task Problems page that extracts repo-local `path:line[:column]` anchors from captured task output. The page has independent selection/scroll, enters page history, and Enter opens the selected location through the existing editor handoff. It deliberately avoids severity parsing, tool-specific diagnostics formats, task history search, and persistence.
106. Task Problems handoff
   - Status: implemented.
   - Why: after task output anchors can be opened, users still need to hand off one failing location or the compact Problems list into AI/chat without manually selecting terminal text.
   - Surface: `copy problem` copies the selected Task Problems entry, and `copy problems` copies every current Task Problems entry in output order. Both reuse existing clipboard configuration and preserve page, selection, scroll, Review Scope, and task state. The feature deliberately avoids severity parsing, tool-specific diagnostics formats, task history search, persistence, and `save problems`.
107. Source File Page
   - Status: implemented.
   - Why: to replace IDE-like workflows, users need to inspect source around a build/test/lint problem without leaving the TUI, even when that file is not changed in the current Review Scope.
   - Surface: `view problem` opens a read-only Source File Page for the selected Task Problems entry. The page renders repo-relative path, line numbers, a target-line marker, scrolls with existing movement keys, supports back/forward history, and keeps Enter on Task Problems as external editor open. It deliberately avoids editing, syntax highlighting, diagnostics parsing, history search, source-view persistence, and task lifecycle changes.
108. Source File Page find
   - Status: implemented.
   - Why: once source previews exist, users need to jump to a nearby symbol or text without manually scrolling through a full source file.
   - Surface: Source File Page supports `find TEXT`, `next match`, and `prev match` with source-page-local `source_find_text`. Search is case-insensitive plain text, updates the Source File Page target line, wraps for repeated matches, and deliberately avoids syntax-aware search, cross-file search, persistence, diagnostics parsing, and editing.
109. Source File Page copy-line
   - Status: implemented.
   - Why: after users jump from task problems into source previews, they need a lightweight way to hand off the current source location without leaving the TUI.
   - Surface: Source File Page supports `copy line` for the current target-line marker. It copies the repo-relative `path:line` anchor through the configured copy command, preserves Source File Page path/line/scroll and task state, and deliberately avoids source snippets, multi-line selection, editing, and File Detail line-resolution changes.
110. Task diagnostics enrichment
   - Status: implemented.
   - Why: Problems should scan more like an IDE panel after task-output anchors exist; users need to distinguish errors, warnings, codes, and messages without reading full logs.
   - Surface: Task Problems now enriches extracted repo-local anchors with generic optional severity, code, and cleaned message facts from common log text. Rows render compact labels such as `ERROR TS2322`, and `copy problem` / `copy problems` include diagnostic facts while preserving raw summary fallback. It deliberately avoids severity sorting, text query filtering, tool-specific parser registries, diagnostics persistence, history search, and changed-file integration.
111. Task Problems severity filter
   - Status: implemented.
   - Why: after severity labels exist, users need to focus on blocking errors or warnings without manually scanning every Problems row.
   - Surface: Task Problems supports `problems errors`, `problems warnings`, `problems info`, `problems note`, and `problems all`. Filtering preserves task-output order and applies to rendered rows, movement, Enter/open, `view problem`, `copy problem`, and `copy problems`. The filter is page-local and restored through page history snapshots. It deliberately avoids severity sorting, text query filtering, task history search, diagnostics persistence, and tool-specific parser registries.
112. Task Problems severity counts
   - Status: implemented.
   - Why: after severity filtering exists, users need immediate visible counts to decide whether to focus errors, warnings, or unknown anchors.
   - Surface: Task Problems header now shows compact counts for the currently visible list, such as `2 errors, 1 warning, 1 unknown`. Counts use the same filtered list as movement/open/copy and deliberately avoid total-vs-filtered aggregate state, severity sorting, persistence, and new commands.
113. Source File Page source-snippet handoff
   - Status: implemented.
   - Why: after users jump from Problems into source previews, AI/review handoff often needs nearby source context rather than only `path:line`.
   - Surface: Source File Page supports `copy source`, copying Markdown headed by `path:line` with up to three source lines before/after the target and a `>` marker on the target line. The feature preserves page state and deliberately avoids multi-line interactive selection, syntax highlighting, editing, cross-file snippets, and persistence.
114. Task Problems severity sort
   - Status: implemented.
   - Why: after severity labels, filters, and counts exist, users need an explicit way to prioritize blocking errors without losing the default task-output timeline.
   - Surface: Task Problems supports `problems sort severity` and `problems sort output`. The default remains task-output order; severity sort groups the visible list by `error`, `warning`, `info`, `note`, then unknown while preserving output order within each bucket. Sorting applies after filtering and drives rendered rows, movement, Enter/open, `view problem`, `copy problem`, and `copy problems`. The sort mode is page-local and restored through page history snapshots, and deliberately avoids persistence, text query filtering, configurable severity order, and diagnostics history.
115. Source File Page adjustable source context
   - Status: implemented.
   - Why: after `copy source` exists, a fixed three-line snippet is sometimes too narrow for AI handoff and sometimes too noisy for a small syntax failure.
   - Surface: Source File Page supports `source context N` to set the `copy source` context radius for the current source page. New Source File Pages default to 3, page history restores the radius, the header shows `context: N`, and values are validated as non-negative integers clamped to a practical maximum. It deliberately avoids interactive multi-line selection, syntax-aware expansion, cross-file snippets, editing, and persistence.
116. Problem Context handoff
   - Status: implemented.
   - Why: after Problems, Source File Page, adjustable source snippets, and diff handoff exist, users need one command that packages the failing point for AI/chat without manually copying three separate pieces.
   - Surface: `copy problem context` works from Task Problems and Source File Page. It copies focused Markdown with problem facts when available, source context around the target line, and same-file diff from the current Review Scope when that file is changed; if there is no matching changed file, it states `No diff in current review scope.`. The feature reuses `cr.ui.task_problems`, `cr.ui.source_file`, `cr.review.data`, and `cr.review.snippet`, adds `cr.ui.problem_context` only for pure Markdown assembly, and deliberately avoids batch saving, cross-file dependency collection, full prompt templates, task transcripts, diagnostics persistence, and source editing.
117. Task Problems text filter
   - Status: implemented.
   - Why: after severity filter/sort, large task outputs can still have too many same-severity rows; users need to narrow Problems by path, code, or message without searching raw logs.
   - Surface: Task Problems supports `problems find TEXT` and `problems clear find`. Query filtering is case-insensitive plain text over path, `path:line[:column]` location, summary, severity, code, and message. It applies after severity filtering and before sorting, drives rendered rows, movement, Enter/open, `view problem`, `copy problem`, `copy problems`, and `copy problem context`, and is restored through page history snapshots. It deliberately avoids regex, raw task-output history search, source-content search, persistence, and tool-specific diagnostic parsers.
118. Source File Page range selection
   - Status: implemented.
   - Why: after adjustable `copy source` and `copy problem context`, users still need to hand off an exact method, branch, or multi-line expression from a problem source preview without over-copying nearby context.
   - Surface: Source File Page supports `source select START END` and `source clear selection`. Selection is page-local, normalized into ascending line order, restored through page history, shown in the header as `selection: A-B`, and rendered with `*` markers while the target line keeps `>`. When selection is active, `copy source` copies only the selected source range; without selection it keeps the existing context-radius copy behavior. It deliberately avoids source editing, syntax-aware expansion, cross-file selection, mouse/shift selection, multi-range selection, and persistence.
119. Problem Context save command
   - Status: implemented.
   - Why: after `copy problem context`, users need durable handoff files for remote terminals, unreliable clipboards, and AI/chat workflows where the focused failing context should be opened from disk.
   - Surface: `save problem context [PATH]` works from Task Problems and Source File Page. It saves the same Markdown generated by `copy problem context`, defaulting to `.cr/handoff/problem-context.md`, reports empty-context, unreadable-source, and write-failure states, and preserves current page, selection, Review Scope, and task state. It deliberately avoids batch saving, task transcript persistence, prompt templates, cross-file dependency discovery, and workspace-state persistence.
120. Task Problems result grouping
   - Status: implemented.
   - Why: after query/severity/sort, large task outputs can still scan like a flat log-derived list; users need file-level structure to see which files dominate the current problem set.
   - Surface: Task Problems supports `problems group file` and `problems group none`. Grouping is render-only, applies after severity filtering, text filtering, and sorting have produced the visible list, shows file headers plus `group: file` in the header, restores through page history, and preserves the same visible `TaskProblem` list for movement, Enter/open, `view problem`, `copy problem`, `copy problems`, `copy file problems`, `copy problem context`, and `save problem context`. It deliberately avoids collapsible groups, group-level selection state, external quick-fix actions, tool-specific diagnostic trees, and persisted grouping preferences.
121. Source File Page mark selection
   - Status: implemented.
   - Why: after exact `source select START END` exists, repeated source handoff still feels too manual when users are already navigating by target line, find, or scroll.
   - Surface: Source File Page supports `source mark`, `source select to`, and `source clear mark`. `source mark` records the current target line, `source select to` sets the selected range between the mark and current target line with direction normalized, and `source clear mark` clears only the mark. The mark is page-local, shown as `mark: N`, restored through page history, cleared on newly opened source pages, and deliberately avoids syntax-aware expansion, mouse/shift selection, multiple marks, cross-file marks, editing, and persistence.
122. Task Problems file-level handoff
   - Status: implemented.
   - Why: after file grouping, users often need the middle handoff shape: all visible diagnostics for the selected file, without copying one isolated problem or the entire build output.
   - Surface: Task Problems supports `copy file problems`. It uses the same current visible list as movement/open/copy, picks the selected problem's path as the file key, copies only visible problems from that file, and preserves page, selection, filters, sort, grouping, Review Scope, and task state. It deliberately avoids external quick-fix execution, collapsible group state, diagnostics persistence, tool-specific parser changes, and task-history browsing.
123. Task Problems file-group navigation
   - Status: implemented.
   - Why: after file grouping and file-level handoff, users still need to move through large Problems lists by file rather than stepping through every individual diagnostic.
   - Surface: Task Problems supports `next problem file` and `prev problem file`. The commands use the same current visible list as movement/open/copy, jump to the first visible problem in the next or previous file, preserve page, filters, sort, grouping, Review Scope, and task state, and show a Chinese edge message at the first or last visible file. They deliberately avoid collapsible groups, selectable headers, group-level state, quick-fix execution, diagnostics persistence, and tool-specific parser changes.

124. File Detail review queue dock
   - Status: implemented.
   - Why: after `docs/product-goal.md` clarified the ideal workflow as reading code while keeping the changed-file queue visible, File Detail needed a small dock so users do not have to return to Changed Files just to remember nearby files and review progress.
   - Surface: File Detail renders a compact Changed Files dock at the bottom when enough vertical space is available. It shows current position, nearby visible files, seen/todo, note markers, source labels, and change summaries using existing workspace facts. It deliberately avoids independent dock selection, persistence state, mouse interaction, configurable dock height, and pane focus.
125. Task output tail handoff
   - Status: implemented.
   - Why: after full task output copy/save and Problems handoff existed, users still needed a compact failure-tail package for logs that are too long to paste wholesale and not fully represented by parsed Problems.
   - Surface: `copy task tail [N]` copies current task output tail Markdown, defaulting to 40 lines and allowing a positive copy line count. `save task tail [PATH]` saves the default 40-line tail to `.cr/handoff/task-output-tail.md` or a requested path. It deliberately avoids task history browsing, log filters, parser changes, capture-capacity changes, configurable defaults, and workspace persistence.
126. Source File current-symbol hint
   - Status: implemented.
   - Why: after Problems can open Source File and copy source context, users still need to know what function/method/class a failing line belongs to without manually scanning the surrounding code.
   - Surface: Source File Page shows a best-effort `symbol: ...` header label for the target line and `copy source` includes the same `Symbol: ...` metadata for both context snippets and selected ranges. It deliberately avoids language-server dependencies, syntax-aware range expansion, source editing, new persisted state, and File Detail changes.
127. Source File symbol range selection
   - Status: implemented.
   - Why: after current-symbol hints, users still had to manually read line numbers to hand off the whole enclosing method or function from a problem source preview.
   - Surface: Source File Page supports `source select symbol` plus aliases `select source symbol` and `source symbol`. The command selects the innermost best-effort outline symbol containing the current source line, shows the selected symbol label and range, redraws the Source File selection markers, and composes with existing `copy source` so the selected method/function range is copied with `Symbol: ...` metadata. It deliberately avoids language-server dependencies, complete syntax parsing, automatic copy side effects, source editing, cross-file selection, persistence, and File Detail changes.
128. Task Output first-problem handoff
   - Status: implemented.
   - Why: after build/test/lint failures, users often start on Task Output and need to jump to the first parseable failure or hand its context back to AI without first opening the full Problems page.
   - Surface: Task Output supports `view problem`, `copy problem context`, and `save problem context [PATH]` against the first visible parsed task problem. The selection rule reuses existing task problem extraction, filter, query, and sort state, always chooses index 0 on Task Output because that page has no problem row selection, and leaves Task Problems selected-row behavior unchanged. It deliberately avoids new parsers, automatic failure navigation, task history browsing, persisted diagnostics, quick fixes, source editing, and changing Problems page selection.
129. File Detail source bridge
   - Status: implemented.
   - Why: after Source File gained symbol hints and symbol-range selection, File Detail still needed a direct in-TUI bridge from the current diff row to surrounding source, instead of forcing users to open an external editor or manually find the same line.
   - Surface: File Detail supports `view source` plus aliases `source view` and `view current source`. The command reuses the same current new-file line mapping as `open line` and `copy line`, opens Source File at that changed file and line, and preserves File Detail in the back stack. It deliberately avoids deleted-only old-source preview, language-service lookup, source editing, automatic symbol selection, and File Detail layout changes.
130. File Detail direct source copy
   - Status: implemented.
   - Why: after `view source`, users could stay inside the TUI, but a common handoff still required a page transition before copying source context around the current diff row.
   - Surface: File Detail now supports `copy source` against the current rendered new-file line. It reuses the same line mapping as `open line`, `copy line`, and `view source`, copies Source File-style Markdown with best-effort `Symbol: ...` metadata, and keeps the browser on File Detail. It deliberately avoids deleted-only old-source preview, automatic symbol-range selection, source editing, language-service dependencies, and File Detail layout changes.
131. Direct source symbol copy
   - Status: implemented.
   - Why: after `source select symbol` and File Detail `copy source`, copying the whole enclosing method/function still took multiple steps from File Detail or required mutating Source File selection state.
   - Surface: `copy source symbol` works on Source File and File Detail, with aliases `copy symbol` and `copy current symbol`. It copies the innermost best-effort outline symbol containing the current source line or current File Detail new-file line, uses Source File range Markdown with `Symbol: ...` metadata, and does not mutate source selection state or navigate pages. It deliberately avoids language-server dependencies, full syntax parsing, deleted-only old-source preview, automatic selection state changes, and source editing.
132. Problem context task output excerpt
   - Status: implemented.
   - Why: after Task Output first-problem handoff and Problems handoff existed, users still had to manually copy the nearby build log line to preserve compiler wording or failure context before giving the package back to AI.
   - Surface: `copy problem context` and `save problem context` include a compact `Task Output` excerpt when run from Task Output or Task Problems. The excerpt is centered on the selected/first problem's captured output line and is omitted for Source File Page contexts. It deliberately avoids new parsers, task history browsing, full log export, configurable excerpt size, source editing, and quick fixes.
133. Task Output problem selection
   - Status: implemented.
   - Why: after Task Output could hand off a problem with source, diff, and task-output context, multi-error logs still forced users into Task Problems just to change which error `view problem` or `copy problem context` targeted.
   - Surface: Task Output supports `next problem` and `prev problem`, shows the selected problem index/location in the page header, and uses that selected problem for `view problem`, `copy problem context`, and `save problem context`. It deliberately avoids new parsers, multi-select operations, persisted diagnostic selection, task history browsing, source editing, quick fixes, and language-server integration.
134. Source arrow-property symbol recognition
   - Status: implemented.
   - Why: after direct `copy source symbol`, Source File and File Detail still missed common ArkTS callback fields such as `private onTap = () => { ... }`, forcing manual line-range selection for handoff.
   - Surface: the lightweight source outline recognizes class/struct/interface field arrow functions as method-like symbols, including access modifiers, `static`, `readonly`, and function-type annotations. Existing Source File current-symbol hints, `source select symbol`, and `copy source symbol` benefit without new commands. It deliberately avoids a full TypeScript/ArkTS parser, language-server dependencies, cross-file resolution, persisted state, source editing, and quick fixes.
135. Source File symbol navigation
   - Status: implemented.
   - Why: after Source File could show and copy current symbols, reading long ArkTS files still required line-by-line scrolling or text search to inspect nearby functions around a build problem.
   - Surface: Source File supports `next symbol` and `prev symbol`, jumping to adjacent recognized source symbol start lines and recentring the page. It reuses the existing lightweight outline and current symbol header. It deliberately avoids an outline tree panel, language-server dependencies, cross-file navigation, persisted symbol selection, source editing, and quick fixes.
136. Source selection problem context
   - Status: implemented.
   - Why: after Source File could select a range or symbol, `copy problem context` still ignored that selected block and only copied a small line-context window, forcing users to copy source separately for AI handoff.
   - Surface: Source File `copy problem context` and `save problem context` use the active selected source range when one exists, while preserving existing line-context behavior when there is no selection. Task Output and Task Problems context generation is unchanged. It deliberately avoids automatic symbol selection side effects, multi-file dependency collection, parser/language-server dependencies, source editing, quick fixes, and new persisted state.
137. File Detail problem context handoff
   - Status: implemented.
   - Why: File Detail is the main place users read a changed file, but packaging the current diff row as AI handoff context still required jumping to Source File or copying source and diff separately.
   - Surface: File Detail now supports `copy problem context` and `save problem context [PATH]` against the current rendered new-file line. The bundle includes source context, best-effort current symbol metadata, and the same-file diff while keeping the user on File Detail with scroll preserved. It deliberately avoids language-server lookup, automatic symbol selection, deleted-only old-source preview, source editing, quick fixes, multi-file dependency collection, and new persistent state.
138. Task problem diff jump
   - Status: implemented.
   - Why: after build/test/lint problems could jump to Source File and package source+diff handoff, users still had to manually return to Changed Files and find the corresponding file when they wanted to inspect the changed hunk behind a failure.
   - Surface: Task Output and Task Problems now support `view problem diff` for the selected/current problem. If the problem path exists in the current review scope, cr opens File Detail for that file and scrolls to the rendered row for the problem line when visible. If the file is not changed in the current scope, cr reports that no diff is available. It deliberately avoids synthetic diffs for unchanged files, language-server lookup, quick fixes, parser changes, source editing, and cross-file dependency collection.
139. Source accessor and override symbol recognition
   - Status: implemented.
   - Why: Source File and File Detail handoff now rely on the lightweight outline, but common ArkTS/TS member shapes such as `override aboutToAppear()` and `get title()` were treated as only the outer class, forcing users to manually select ranges before copying useful context.
   - Surface: the lightweight outline now recognizes `override` class/struct/interface methods and `get`/`set` accessors as method-like symbols. Existing current-symbol hints, `source select symbol`, `copy source symbol`, and source/problem context handoff benefit without new commands. It deliberately avoids a full parser, decorator semantics, cross-file resolution, language-server dependencies, source editing, and quick fixes.
140. Source generic symbol recognition
   - Status: implemented.
   - Why: generic ArkTS/TS shapes such as `createModel<T>()`, `function parse<T>()`, and `const load = <T>() => {}` were still treated as only the outer class or no symbol, which made source-symbol copy and problem-context handoff less precise.
   - Surface: the lightweight outline now recognizes generic methods, top-level functions, top-level arrow functions, and field arrow functions as the same existing method/function symbols. Existing current-symbol hints, `source select symbol`, `copy source symbol`, and handoff context benefit without new commands. It deliberately avoids parsing generic semantics, type resolution, cross-file dependency collection, language-server dependencies, source editing, and quick fixes.
141. Task Output match handoff
   - Status: implemented.
   - Why: after Task Output supported `find` and tail handoff, long build logs still forced users to manually copy the one relevant failure line plus a little surrounding wording before handing context back to AI.
   - Surface: `copy task match` and `save task match [PATH]` package the current Task Output find focus as Markdown with up to three lines before and after, line numbers, and a `>` marker on the focused line. The save default is `.cr/handoff/task-output-match.md`. It deliberately requires a prior `find TEXT` and avoids new parsers, task history browsing, multi-match export, configurable excerpt sizes, source editing, and quick fixes.
142. Source exported arrow symbol recognition
   - Status: implemented.
   - Why: top-level TS/ArkTS helpers are often written as `export const load = (...) => {}`, but the lightweight outline treated those bodies as no symbol, making `copy source symbol` and symbol-based handoff fall back to coarse context.
   - Surface: the lightweight outline now recognizes exported top-level `const`, `let`, and `var` arrow declarations as function symbols. Existing Source File/File Detail current-symbol hints, symbol navigation, `copy source symbol`, `source select symbol`, and problem-context handoff benefit without new commands. It deliberately avoids a full parser, export graph resolution, default export handling, source editing, quick fixes, and language-server integration.
143. Source default export symbol recognition
   - Status: implemented.
   - Why: TS/ArkTS modules often use `export default class Foo` or `export default function makeFoo()`, but the lightweight outline treated those named declarations as no symbol, widening source-symbol handoff for module entrypoints.
   - Surface: the lightweight outline now recognizes named default-exported containers and functions as the same class/struct/interface/function symbols as ordinary exports. Existing Source File/File Detail symbol hints, symbol navigation, `copy source symbol`, `source select symbol`, and problem-context handoff benefit without new commands. It deliberately avoids anonymous default exports, default arrow expressions, export graph resolution, source editing, quick fixes, and language-server integration.
144. Task Problems list save handoff
   - Status: implemented.
   - Why: after `copy problems` and `copy file problems`, users still needed durable handoff files for filtered/sorted Problems lists when clipboard handoff is unreliable or the list is too long to paste immediately.
   - Surface: Task Problems supports `save problems [PATH]` and `save file problems [PATH]`. Defaults are `.cr/handoff/task-problems.md` and `.cr/handoff/task-problems-file.md`; both use the current visible filtered/sorted list, and the file command scopes to the selected problem's path. It deliberately avoids diagnostics persistence, task history browsing, parser changes, batch source/diff expansion, source editing, and quick fixes.
145. Source handoff save
   - Status: implemented.
   - Why: after Source File and File Detail supported `copy source` and `copy source symbol`, clipboard-only handoff still failed for remote terminals, long snippets, or review packages that should survive after the session.
   - Surface: Source File and File Detail support `save source [PATH]` and `save source symbol [PATH]`. Defaults are `.cr/handoff/source.md` and `.cr/handoff/source-symbol.md`; saved Markdown matches the existing copy commands, including selected source ranges and best-effort `Symbol: ...` metadata. It deliberately avoids editing, language-service lookup, cross-file dependency expansion, new persisted review state, and changing Source File selection from File Detail.
146. Source File diff bridge
   - Status: implemented.
   - Why: after a build/test/lint problem opens Source File, users often inspect source first and then need the corresponding changed-file diff without returning to Task Problems.
   - Surface: Source File supports `view diff` and existing `view problem diff` to open File Detail for the current source path when that file exists in the active review scope. The command scrolls to the current source line when the rendered diff exposes it, otherwise opens the changed file and reports that the exact line is not visible. It deliberately avoids new problem-origin state, synthetic diffs for unchanged files, language-service lookup, source editing, quick fixes, and cross-file dependency expansion.
147. Source File problem stepping
   - Status: implemented.
   - Why: after Source File opens a build/test/lint problem, scanning the next failure still required returning to Task Problems or Task Output instead of staying in source-reading mode.
   - Surface: Source File supports `next problem` and `prev problem` to move the selected parsed task problem and update the source preview to that problem's path and line. The page stays on Source File, and back navigation still returns to the original prior page rather than walking through every visited problem source. It deliberately avoids persisted diagnostics, new problem-origin state, multi-select review, quick fixes, source editing, and language-service integration.
148. Source File problem header
   - Status: implemented.
   - Why: after Source File could step through task problems, users still had to remember the selected diagnostic while reading source, especially across multiple similar failures.
   - Surface: Source File shows a compact `problem: ...` header label when the selected parsed task problem exactly matches the current source path and target line. The label includes problem position, diagnostic label, and message/summary, and it is hidden after source navigation moves to a non-matching line. It deliberately avoids diagnostics persistence, new problem-origin state, language-service lookup, quick fixes, source editing, and multi-problem panels.
149. Source File current problem copy
   - Status: implemented.
   - Why: after Source File showed the current problem header, users still needed a small handoff that copies just that diagnostic without packaging source and diff context.
   - Surface: Source File supports `copy problem` for the currently displayed problem, using the same exact path/line match as the `problem: ...` header. If the source target no longer matches the selected parsed task problem, the command refuses to copy a stale diagnostic. Existing Task Output and Task Problems `copy problem` behavior is unchanged. It deliberately avoids diagnostics persistence, batch copy, quick fixes, source editing, language-service lookup, and new problem-origin state.
150. Current problem save handoff
   - Status: implemented.
   - Why: after `copy problem`, users still needed durable single-diagnostic handoff files for remote terminals, unreliable clipboards, and review loops where the focused issue should survive outside the TUI session.
   - Surface: Task Output, Task Problems, and Source File support `save problem [PATH]`. The default is `.cr/handoff/task-problem.md`, the saved Markdown matches `copy problem`, and Source File uses the same exact path/line match as the `problem: ...` header so stale source positions are refused. It deliberately avoids batch saving, source/diff expansion, diagnostics persistence, quick fixes, source editing, parser changes, and task-history browsing.
151. Current problem diff handoff
   - Status: implemented.
   - Why: after `view problem diff` and `copy problem context`, users still needed a focused middle handoff: just the changed-file diff for the selected build/test problem, without navigating away or packaging source and task output.
   - Surface: Task Output, Task Problems, and exact-match Source File current problems support `copy problem diff` and `save problem diff [PATH]`. The save default is `.cr/handoff/problem-diff.md`; commands use the current review scope and refuse problems whose files are not changed. It deliberately avoids synthetic diffs, source/task-output packaging, parser changes, diagnostics persistence, quick fixes, source editing, and task-history browsing.
152. Review notes save handoff
   - Status: implemented.
   - Why: after Review Notes could be written, shown, filtered, and copied, users still needed durable notes handoff files for longer review sessions, remote terminals, or AI follow-up prompts.
   - Surface: `save notes [PATH]` saves the same ordered Review Notes summary as `notes` / `copy notes`, defaulting to `.cr/handoff/review-notes.md`. Empty note sets refuse to write and report `No review notes to save.` It deliberately avoids filtered-save syntax, new note metadata, note editing changes, workspace persistence changes, and a new export format.
153. Autonomous P0 goal protocol
   - Status: implemented.
   - Why: the product needs Codex to keep choosing and implementing the next highest-value P0 without waiting for manual feature-by-feature instructions.
   - Surface: `docs/product-goal.md` now includes a short reusable Goal prompt plus explicit P0 selection rules. Future turns should prioritize the AI-code handoff loop, choose one minimum vertical slice, verify it, update docs, commit, and push. It deliberately avoids changing runtime behavior, adding a roadmap tracker, introducing GUI scope, or weakening the current terminal-first product boundary.
154. File Detail source symbol view
   - Status: implemented.
   - Why: after File Detail could open the current source line and copy the enclosing source symbol, reading or handing off the full function still required a two-step `view source` then `source select symbol` flow.
   - Surface: File Detail supports `view source symbol`, which opens Source File at the current new-file line and selects the enclosing lightweight source symbol. Deleted-only rows stay on File Detail with the existing no-new-line message, and lines without a recognized symbol still open in Source File without creating a fake selection. It deliberately avoids language-server lookup, outline panels, source editing, quick fixes, cross-file dependency expansion, and new persisted state.
155. Source declaration symbol recognition
   - Status: implemented.
   - Why: abstract classes and interfaces often contain declaration-only members, but the lightweight outline either missed `abstract` members or let declaration signatures stretch over following concrete methods, making symbol handoff imprecise.
   - Surface: the lightweight outline now recognizes abstract methods/accessors and treats function-like declarations without bodies as one-line symbols. Existing `view source symbol`, `copy source symbol`, `source select symbol`, and problem/source context handoff benefit without new commands. It deliberately avoids full parsing, type resolution, inheritance lookup, source editing, quick fixes, language-server integration, and cross-file expansion.

## Next P0 candidates

The long-term product direction is a terminal-first AI change workbench for high-frequency review, navigation, build, problem triage, and handoff. Current next P0 candidates should come from real usage friction in this order:

1. Build -> Problems -> Source closure: continue only where selected Task Output problem handoff or problem-to-diff/source navigation still leaves repeated failure triage slow.
2. Source File Page syntax-aware range expansion: continue only where source symbol navigation/copy/handoff still misses common ArkTS block shapes after arrow fields, accessors, override methods, and generic function-like symbols.
3. File Detail source-context bridge: continue only if users still need faster File Detail -> Source File handoff after direct `copy source`, `copy source symbol`, and File Detail `copy problem context`.
